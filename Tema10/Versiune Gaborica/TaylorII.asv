function [ t, x ] = TaylorII( f, df, a, b, alfa, N )
    h = (b-a)/N;
    t = zeros(1,N+1);
    t(1) = a;
    for i = 2:N+1
        t(i) = t(i-1) + h;
    end
    x = zeros(1,N);
    x(1) = alfa;
    for i = 1:N
       sum = 0;
       for k = 1:2
           sum = (h^(k-1))/factorial(k) * 
       end
       x(i+1) = x(i) + h * 
    end
end

