function [ t, x ] = Euler( f, a, b, alfa, N )
    h = (b-a)/N;
    t = zeros(1,N+1);
    t(1) = a;
    for i = 2:N+1
       t(i) = t(i-1)+h; 
    end
    x = zeros(1,N);
    x = alfa;
    for i = 1:N
       x(i+1) = x(i) + h*f(t,x(i));
    end
end

